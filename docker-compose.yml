version: '3.5'

services:
  app:
    build:
      context: ./docker/app
      dockerfile: Dockerfile
    image: sarahman.com/commission-calculator
    container_name: sarahman-commission-calculator-app
    volumes:
      - .:/var/www/html
